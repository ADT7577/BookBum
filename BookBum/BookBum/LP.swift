//
//  ContentView.swift
//  BookBum
//
//  Created by Alexsa Tolentino on 4/4/23.
//

import SwiftUI

struct LP: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("Hello, world!")
        }
        .padding()
    }
}

struct LP_Previews: PreviewProvider {
    static var previews: some View {
     LP()
    }
}
